import React from "react";

const header = () => {
  return (
    <div>
      <nav className="navbar">
        <a href="#">Categorias</a>
        <a href="#">Cadastro</a>
        <a href="#">Carrinho</a>
      </nav>
    </div>
  );
};

export default header;
